# Repo A: .github/workflows/use-external.yml
on:
  workflow_dispatch:

jobs:
  call-external:
    uses: username/repo-b/.github/workflows/reusable.yml@main
    with:
      message: "Triggered from Repo A"
